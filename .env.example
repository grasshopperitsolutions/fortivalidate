# Copy this file to .env.local and fill in your Gemini API key.
# NEVER commit .env.local to source control.
#
# NOTE (Future refactor): Move AI calls to a Vercel API route (/api/analyze)
# so the key is never exposed in the client bundle. The askAiService.ts is
# already structured to make this a 1-line URL + headers swap inside callProvider().
VITE_AI_API_KEY=your_gemini_api_key_here
